# LocationDataAnalysis
This script analyzes location data from google maps and extracts information about specific places. It takes JSON data as input and generates a CSV file with relevant information.
